
<br>

How to use Helm to fetch the details of an already-installed release.

`helm show` reads from a *chart* in a repository. `helm get` reads from a *release* in the cluster — what was actually deployed, with the values that were actually applied.
