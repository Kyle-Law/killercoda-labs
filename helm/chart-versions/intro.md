
<br>

Helm has already been installed and the `podinfo` repository has been added.

A chart repository holds many versions of the same chart. This scenario covers finding a *specific* version and installing it, rather than always taking the newest.
