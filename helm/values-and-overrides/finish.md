
<br>

### WELL DONE!

You solved this challenge.

Next: `packaging/helm-values-precedence` covers what happens once overrides start to conflict — layering multiple `-f` files, `--set` vs `-f` precedence, and the sharp edges of `--set` itself (`--set-string`, `--set-file`, list indices).
