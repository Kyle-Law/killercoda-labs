
Helm has already been installed, and a `mock-app` release is running in the `dev-ns` namespace.

Learn how to override chart values — the mechanism that makes one chart reusable across environments.
