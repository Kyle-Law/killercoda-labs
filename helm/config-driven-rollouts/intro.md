
Helm has already been installed, and a `mock-app` release is running in `dev-ns`.

Learn why updating a ConfigMap through Helm **doesn't** restart the Pods that consume it — and the standard chart pattern that makes it happen automatically.
