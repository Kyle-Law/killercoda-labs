
<br>

Four release-engineering skills beyond a basic rolling update and `rollout undo`: recovering a specific historical revision (not just "the previous one"), choosing an update strategy to match a real constraint, batching several changes into a single revision, and the one release pattern a Deployment doesn't give you for free — blue/green.

Every step here builds a task, not a solution — work it out yourself first, use the **Tip** if you're stuck, and check the **Solution** only once you've tried.
