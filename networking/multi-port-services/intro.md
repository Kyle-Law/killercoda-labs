
<br>

Three Service features beyond the basics already covered in the Services & DNS troubleshooting lab:

1. Referencing a container's port **by name** instead of by number
2. Exposing more than one port on a single Service (which comes with a naming rule the API server enforces)
3. `ExternalName` — a Service that's really just a DNS alias, with no selector or endpoints at all
